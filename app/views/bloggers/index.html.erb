<h1>Bloggers:</h1>
<ul><% @bloggers.each do |blogger|%>
    <li><%= link_to(blogger.name, blogger_path(blogger))%></li>
    <ul>
        <li><%= pluralize(blogger.post_count,"Post") %></li>
        <li><%= pluralize(blogger.like_count,"Like") %></li>
    </ul>
<% end %></ul>